spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/teach_learning
    username: app_user          
    password: kazu1112      
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate       
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true
